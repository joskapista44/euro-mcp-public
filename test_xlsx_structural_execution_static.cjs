'use strict'
const assert=require('assert'),{executeVerified}=require('./xlsx-structural-execution.cjs')
const x=n=>({value:n}),z=()=>({value:'',formula:null,dataType:'blank'})
;(async()=>{let dispatches=0,reads=0,inspects=0,dispatchOp=null;const api={inspect:async()=>{inspects++;return {ok:true,authority:'LIVE_READ',sheets:[{name:'S',usedRange:'A1:B2'}]}},readRange:async spec=>{reads++;return spec.range==='A2:B2'?{ok:true,authority:'LIVE_READ',cells:[[x(3),x(4)]]}:{ok:true,authority:'LIVE_READ',cells:[[z(),z()],[x(3),x(4)]]}},dispatchStructural:async op=>{dispatches++;dispatchOp=op;return {ok:true,authority:'DISPATCH_ONLY',operation:op.type}}};const r=await executeVerified(api,{type:'rows.insert',sheet:'S',range:'A2',count:1,anchor:2,axis:'rows'});assert.equal(r.ok,true);assert.equal(r.authority,'LIVE_VERIFY');assert.equal(dispatches,1);assert.equal(reads,2);assert.equal(inspects,2);assert.equal(r.window.before,'A2:B2');assert.equal(r.window.after,'A2:B3');assert.equal(r.window.dispatch,'A2:B2');assert.equal(r.dispatchRange,'A2:B2');assert.equal(dispatchOp.range,'A2:B2');assert.equal(dispatchOp.requestedRange,'A2');console.log('XLSX STRUCTURAL EXECUTION STATIC: PASS')})().catch(e=>{console.error(e);process.exitCode=1})

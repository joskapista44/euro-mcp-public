'use strict'
const assert=require('assert'),p=require('./xlsx-persistent-session.cjs')
process.env.EURO_PLAYWRIGHT_PATH=process.env.EURO_PLAYWRIGHT_PATH||'/home/user/marveen/node_modules/playwright'
const FILE_ID=Number(process.env.EURO_XLSX_PERSISTENT_FILE_ID||1231187)
function credentials(){const vault=require('/home/user/marveen/dist/web/vault.js');return {url:process.env.EURO_NEXTCLOUD_URL||'https://mt-server.eu',user:process.env.EURO_NEXTCLOUD_USER||'elliot',pass:vault.getSecret('Elliot_nc_pass','xlsx-persistent-range-move-live-acceptance')}}
;(async()=>{const c=credentials(),sheet='EURO RM '+String(Date.now()).slice(-7);const setup=await p.executeAgentTaskInPersistentSession({fileId:FILE_ID,credentials:c,task:{operations:[{intent:'create_sheet',name:sheet},{intent:'write_range',sheet,range:'A1:B2',values:[[11,22],[33,null]],formulas:[[null,null],[null,'=A1+B1']]}]}});assert.equal(setup.ok,true);const task={operations:[{intent:'move_range',sheet,range:'A1:B2',targetRange:'D1:E2'}]};const r=await p.executeRangeMoveTaskInPersistentSession({fileId:FILE_ID,credentials:c,task});console.log(JSON.stringify(r,null,2));assert.equal(r.ok,true);assert.equal(r.authority,'LIVE_VERIFY');assert.equal(r.noOp,false);assert.equal(r.receipt[0].status,'APPLIED');assert.equal(r.wholeTaskVerification.ok,true);assert.equal(r.persistentSession.writes,1);assert.equal(r.persistentSession.persistenceBarrier?.ok,true);console.log('XLSX PERSISTENT RANGE MOVE LIVE ACCEPTANCE: PASS')})().catch(e=>{console.error(e);process.exitCode=1})

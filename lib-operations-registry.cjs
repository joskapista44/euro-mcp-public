'use strict'
// Populated by lib.cjs via Object.assign,
// once, right after every helper function/const it defines -- BEFORE lib.cjs requires any of
// the split operations files. A plain object, not a function call: this is what lets the
// operations-*.cjs files stay plain top-level object literals (see their own header comments
// for why that matters for qlty's complexity scoring) while still reaching lib.cjs's helpers
// without a circular require (this file itself requires nothing and exports nothing but the
// bare object -- lib.cjs is the only writer, everyone else only reads after it has run).
module.exports = {}

'use strict'
const crypto=require('crypto')
function normFormula(v){return typeof v==='string'&&v.trim()?v.replace(/\s+/g,'').toUpperCase():null}
function coordinate(v){const n=Number(v);return Number.isInteger(n)&&n>=1?n:null}
function cellState(c){const row=coordinate(c?.row),column=coordinate(c?.column);if(row===null||column===null)return null;const state={row,column};const f=normFormula(c?.formula);if(f)return {...state,formula:f};if(c&&Object.prototype.hasOwnProperty.call(c,'rawValue'))return {...state,value:c.rawValue};if(c&&Object.prototype.hasOwnProperty.call(c,'value'))return {...state,value:c.value};return {...state,value:c?.displayText??null}}
function snapshot(read){if(!read?.ok||read.authority!=='LIVE_READ'||!Array.isArray(read.cells)||!read.cells.length)return null;const out=[];for(const row of read.cells){if(!Array.isArray(row)||!row.length)return null;const mapped=[];for(const cell of row){const state=cellState(cell);if(!state)return null;mapped.push(state)}out.push(mapped)}const width=out[0].length;if(out.some(row=>row.length!==width))return null;return out}
function fingerprint(read){const s=snapshot(read);if(!s)return null;return crypto.createHash('sha256').update(JSON.stringify(s)).digest('hex')}
function classify(read,expected){const actual=fingerprint(read);if(!actual)return {ok:false,outcome:'xlsx-structural-precondition-unreadable',authority:'PLAN_ONLY'};if(typeof expected!=='string'||!/^[0-9a-f]{64}$/i.test(expected))return {ok:false,outcome:'xlsx-structural-precondition-invalid',authority:'PLAN_ONLY',actualFingerprint:actual};if(actual.toLowerCase()!==expected.toLowerCase())return {ok:false,outcome:'xlsx-structural-precondition-mismatch',authority:'LIVE_READ',expectedFingerprint:expected.toLowerCase(),actualFingerprint:actual};return {ok:true,outcome:'xlsx-structural-precondition-match',authority:'LIVE_VERIFY',fingerprint:actual}}
module.exports={snapshot,fingerprint,classify}

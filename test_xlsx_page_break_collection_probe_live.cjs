'use strict'
const assert=require('assert/strict'),persistent=require('./xlsx-persistent-session.cjs')
const FILE_ID=Number(process.env.EURO_XLSX_PERSISTENT_FILE_ID||1231187)
process.env.EURO_PLAYWRIGHT_PATH=process.env.EURO_PLAYWRIGHT_PATH||'/home/user/marveen/node_modules/playwright'
function secret(id){const v=require('/home/user/marveen/dist/web/vault.js');const r=v.getSecret(id,'xlsx-page-break-collection-probe');if(!r)throw new Error('vault secret not found: '+id);return r}
const options={url:process.env.EURO_NEXTCLOUD_URL||'https://mt-server.eu',user:process.env.EURO_NEXTCLOUD_USER||'elliot',pass:secret('Elliot_nc_pass'),fileId:FILE_ID,timeoutMs:30000,pollMs:50}
;(async()=>{const r=await persistent.withPersistentXlsxSession(options,async api=>{const p=await api.session.frame.evaluate(()=>{try{const C=window.AscCommonExcel&&window.AscCommonExcel.CRowColBreaks;if(!C)return {ok:false,outcome:'collection-class-unavailable'};const o=new C();let keys=[];for(let x=o,d=0;x&&d++<4;x=Object.getPrototypeOf(x))keys=keys.concat(Object.getOwnPropertyNames(x));keys=[...new Set(keys)].filter(k=>/(break|count|add|remove|get|change)/i.test(k)).sort();let src={};for(const k of keys)try{src[k]=typeof o[k]==='function'?String(o[k]):{type:typeof o[k],value:o[k]}}catch(_){};return {ok:true,methods:src,initialBreaks:typeof o.getBreaks==='function'?o.getBreaks():null}}catch(err){return {ok:false,error:String(err&&err.stack||err)}}});return {ok:!!p?.ok,outcome:p?.ok?'xlsx-page-break-collection-probe':'xlsx-page-break-collection-probe-failed',authority:p?.ok?'LIVE_VERIFY':'LIVE_READ',noOp:true,probe:p}});console.log('XLSX PAGE BREAK COLLECTION PROBE',JSON.stringify(r,null,2));assert.equal(r.ok,true);assert.equal(r.persistentSession?.writes,0);console.log('XLSX PAGE BREAK COLLECTION PROBE: PASS')})().catch(e=>{console.error(e?.stack||e);process.exitCode=1})

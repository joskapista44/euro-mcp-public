'use strict'
const assert=require('assert'),task=require('./xlsx-agent-range-move-task.cjs')
const inv={ok:true,authority:'LIVE_READ',sheets:[{name:'S'}]},before={ok:true,authority:'LIVE_READ',cells:[[{row:1,column:1,value:5}]]},blank={ok:true,authority:'LIVE_READ',cells:[[{value:'',formula:null,dataType:'blank'}]]},target={ok:true,authority:'LIVE_READ',cells:[[{row:1,column:4,value:5}]]}
;(async()=>{let p=task.planTask({operations:[{intent:'move_range',sheet:'S',range:'A1',targetRange:'D1'}]});assert.equal(p.ok,true);p=task.planTask({operations:[{intent:'move_range',sheet:'S',range:'A1',targetRange:'A1'}]});assert.equal(p.ok,false);let inspections=0,reads=0,moves=0;const api={inspect:async()=>{inspections++;return inv},moveRangeVerified:async()=>{moves++;return {ok:true,authority:'LIVE_VERIFY',noOp:false,beforeSource:before}},readRange:async spec=>{reads++;return spec.range==='A1'?blank:target}};let r=await task.executeRangeMoveTask({task:{operations:[{intent:'move_range',sheet:'S',range:'A1',targetRange:'D1'}]},api});assert.equal(r.ok,true);assert.equal(r.authority,'LIVE_VERIFY');assert.equal(moves,1);assert.equal(reads,2);assert.equal(r.wholeTaskVerification.ok,true);assert.ok(inspections>=2);console.log('XLSX AGENT RANGE MOVE TASK STATIC: PASS')})().catch(e=>{console.error(e);process.exitCode=1})

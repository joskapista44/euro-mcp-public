'use strict'
const transport=require('./xlsx-persistent-page-number-order.cjs')
function planTask(task){const op=task?.operations?.[0],value=op?.value!==undefined?op.value:op?.firstPageNumber,ok=task?.operations?.length===1&&op?.intent==='set_first_page_number'&&typeof op.sheet==='string'&&op.sheet.trim()&&(value===null||Number.isInteger(value)&&value>0);return ok?{ok:true,outcome:'xlsx-first-page-number-task-planned',authority:'PLAN_ONLY',operation:{index:op.index??0,intent:op.intent,sheet:op.sheet,firstPageNumber:value}}:{ok:false,outcome:'xlsx-first-page-number-task-invalid',authority:'PLAN_ONLY'}}
async function firstPageNumberObserved(api,op){return api.firstPageNumberObserved(op,false)}
async function executeFirstPageNumberTask(arg1,arg2){const task=arg2||arg1?.task,api=arg2?arg1:arg1?.api;const p=planTask(task);if(!p.ok)return p;const pre=await firstPageNumberObserved(api,p.operation);if(pre.ok&&pre.noOp)return {...pre,outcome:'xlsx-first-page-number-task-already-satisfied',authority:'LIVE_VERIFY'};const r=await api.firstPageNumberObserved(p.operation,true);return {...r,outcome:r.ok?'xlsx-first-page-number-task-live-verified':r.outcome,authority:r.ok?'LIVE_VERIFY':'LIVE_READ'}}
module.exports={planTask,firstPageNumberObserved,executeFirstPageNumberTask}

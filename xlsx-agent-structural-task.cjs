'use strict'
const reader=require('./range-reader.cjs')
const INTENTS=new Set(['insert_rows','delete_rows','insert_columns','delete_columns'])
function planTask(task){if(!task||!Array.isArray(task.operations)||task.operations.length!==1)return {ok:false,outcome:'xlsx-structural-task-single-operation-required',authority:'PLAN_ONLY',writeAllowed:false};const op=task.operations[0];if(!INTENTS.has(op?.intent)||typeof op.sheet!=='string'||!op.sheet.trim()||typeof op.range!=='string')return {ok:false,outcome:'xlsx-structural-task-invalid-operation',authority:'PLAN_ONLY',writeAllowed:false};const parsed=reader.parseA1Range(op.range);if(!parsed)return {ok:false,outcome:'xlsx-structural-task-invalid-range',authority:'PLAN_ONLY',writeAllowed:false};const rows=op.intent.endsWith('_rows');if(rows&&parsed.start.column!==1)return {ok:false,outcome:'xlsx-structural-rows-require-column-a-anchor',authority:'PLAN_ONLY',writeAllowed:false};if(!rows&&parsed.start.row!==1)return {ok:false,outcome:'xlsx-structural-columns-require-row-1-anchor',authority:'PLAN_ONLY',writeAllowed:false};const count=rows?parsed.rows:parsed.columns,type=op.intent==='insert_rows'?'rows.insert':op.intent==='delete_rows'?'rows.delete':op.intent==='insert_columns'?'columns.insert':'columns.delete';return {ok:true,outcome:'xlsx-structural-task-planned',authority:'PLAN_ONLY',writeAllowed:true,operation:{index:0,intent:op.intent,type,sheet:op.sheet,range:parsed.address,count,anchor:rows?parsed.start.row:parsed.start.column,axis:rows?'rows':'columns'}}}
module.exports={planTask}

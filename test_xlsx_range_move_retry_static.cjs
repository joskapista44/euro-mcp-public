'use strict'
const assert=require('assert'),pre=require('./xlsx-structural-precondition.cjs'),retry=require('./xlsx-range-move-retry-token.cjs'),task=require('./xlsx-agent-range-move-task.cjs')
const read=cells=>({ok:true,authority:'LIVE_READ',cells}),cell=(v,row,column)=>({rawValue:v,value:v,displayText:String(v??''),formula:null,row,column}),before=read([[cell(1,1,1)]]),blank=read([[cell(null,1,1)]]),target=read([[cell(1,1,4)]]),op={intent:'move_range',sheet:'S',range:'A1',targetSheet:'S',targetRange:'D1'},token=retry.make(op,before,blank,target)
assert.ok(token);assert.equal(retry.validate(token,op).ok,true);assert.equal(retry.validate(token,{...op,targetRange:'E1'}).outcome,'xlsx-range-move-retry-token-operation-mismatch')
;(async()=>{let moves=0,reads=0,inspects=0;const api={inspect:async()=>{inspects++;return {ok:true,authority:'LIVE_READ',sheets:[{name:'S'}]}},readRange:async spec=>{reads++;return spec.range==='A1'?blank:target},moveRangeVerified:async()=>{moves++;throw new Error('must not dispatch')}};const r=await task.executeRangeMoveTask({task:{operations:[{...op,retryToken:token}]},api});assert.equal(r.ok,true);assert.equal(r.outcome,'xlsx-range-move-task-already-satisfied');assert.equal(r.authority,'LIVE_VERIFY');assert.equal(r.noOp,true);assert.equal(r.writeAllowed,false);assert.equal(r.receipt[0].status,'ALREADY_SATISFIED');assert.equal(r.retryToken,token);assert.equal(moves,0);assert.equal(inspects,2);console.log('XLSX RANGE MOVE RETRY STATIC: PASS')})().catch(e=>{console.error(e);process.exitCode=1})
